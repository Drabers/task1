# Модуль Validation містить методи для перевірки різних типів даних на валідність.module Validation
  # Метод valid_name? перевіряє, чи є передана стрічка валідним ім'ям.  # Валідне ім'я може містити літери, дефіс та пробіли.
  # Максимальна допустима довжина імені - 40 символів.  def self.valid_name?(string)
    # Використовуємо регулярний вираз для перевірки валідності імені.    # ^ - початок стрічки, $ - кінець стрічки
    # [a-zA-Z\- ] - літери, дефіс та пробіли    # {1,40} - довжина від 1 до 40 символів
    !!string.match(/\A[a-zA-Z\- ]{1,40}\z/)  end
  # Метод valid_inn? перевіряє, чи є передана стрічка валідним ІПН (ідентифікаційний номер платника податків).
  # Валідний ІПН повинен бути у форматі дві великі літери, за якими йдуть 10 цифр.  def self.valid_inn?(string)
    # Використовуємо регулярний вираз для перевірки валідності ІПН.    # \A - початок стрічки, \z - кінець стрічки
    # [A-Z]{2} - дві великі літери    # \d{10} - 10 цифр
    !!string.match(/\A[A-Z]{2}\d{10}\z/)  end
  # Метод after_date? перевіряє, чи є передана дата можливою датою народження.
  # Це несувора перевірка на дати, які можуть бути датами народження.  def self.after_date?(date)
    # Припустимо, що ми не дозволяємо майбутні дати.    # Тому дата не може перевищувати сьогоднішню дату.
    today = Date.today    date <= today
  endend
# Приклади використання методів:
# Перевірка валідності імені "Maria-Anna"
puts Validation.valid_name?("Maria-Anna") # => true
# Перевірка валідності імені "John Doe-Smith" (заперечний знак недопустимий у валідному імені)puts Validation.valid_name?("John Doe-Smith") # => false
# Перевірка валідності ІПН "AZ1234543456"
puts Validation.valid_inn?("AZ1234543456") # => true
# Перевірка валідності ІПН "AB123456789" (10 цифр не відповідають формату)puts Validation.valid_inn?("AB123456789") # => false
# Перевірка чи дата 1990-01-01 можлива датою народження
puts Validation.after_date?(Date.parse("1990-01-01")) # => true
# Перевірка чи дата 2050-12-31 можлива датою народження (майбутня дата, тому неприпустима)puts Validation.after_date?(Date.parse("2050-12-31")) # => false
